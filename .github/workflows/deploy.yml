name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Oracle VM
    runs-on: ubuntu-latest

    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ORACLE_SSH_KEY }}" | base64 -d > ~/.ssh/oracle_deploy_key
          chmod 600 ~/.ssh/oracle_deploy_key
          ssh-keyscan -H "${{ secrets.ORACLE_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy on Oracle server
        run: |
          ssh -i ~/.ssh/oracle_deploy_key "${{ secrets.ORACLE_USER }}@${{ secrets.ORACLE_HOST }}" << 'EOF'
            set -e
            cd "${{ secrets.ORACLE_PROJECT_PATH }}"
            git pull origin main
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build -d
            docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
          EOF